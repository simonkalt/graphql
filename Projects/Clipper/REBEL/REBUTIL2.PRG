USE RDINV EXCLU
APPEND FROM rdinv

LREC := LASTREC()
GO TOP
DO WHILE !EOF()
  REPLACE BEQTYAVAIL WITH BRQTYAVAIL
  REPLACE BEPRICE    WITH BRPRICE
  REPLACE BETRANSMIT WITH BRTRANSMIT
  REPLACE BEITEMNO   WITH BRITEMNO
  REPLACE BECASEPACK WITH BRCASEPACK
  ? (RECNO()/lREC)*100
  SKIP
ENDDO
